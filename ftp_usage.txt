FTPConnection ftp = FTPConnection("10.0.0.1", 21, "username", "secret_password");
	
ftp.SetTimeout(2);
	
ftp.SetMaxRetry(5);
	
if(ftp.Connect())
{
	ftp.SendDirectory("secret_directory", "/"); // FIRST ARGUMENT IS DIRECTORY TO SEND, SECOND ARGUMENT IS DIRECTORY ON FTP
}